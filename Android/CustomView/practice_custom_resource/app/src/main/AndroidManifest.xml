<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.me94me.practice_customize_view">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.C1FoundationActivity" />
        <activity android:name=".ui.foundation.cavas.F1CanvasActivity" />
        <activity android:name=".ui.foundation.paint.F2PaintActivity" />
        <activity android:name=".ui.foundation.paint.color.Paint1ColorActivity" />
        <activity android:name=".ui.foundation.paint.effect.Paint2EffectActivity" />
        <activity android:name=".ui.foundation.paint.text.Paint3TextActivity" />
        <activity android:name=".ui.foundation.paint.initialize.Paint3InitializeActivity" />
        <activity android:name=".ui.foundation.drawtext.DrawTextActivity"></activity>
    </application>

</manifest>